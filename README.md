# Telegram Agent Bot

A persistent social agent that lives inside a private Telegram group. It maintains long-term cognitive memories/profiles of group members, executes local tools (moderation, memory, web search, vision OCR), and preserves a consistent personality across different language models. It includes a premium dark-theme web control dashboard.

---

## 🚀 Key Features

* **LLM-Independent Identity**: Core behavior and planning are directed by **Gemma 31B** (Identity/Planner Brain), while casual text generation is delegated to lighter worker models (e.g. **Gemini 3.1 Flash Lite**), preserving personality.
* **Relational & Collective Memory**: Maintains a social graph of user interactions and a shared group history of events and inside jokes, stored in SQLite and accessed on-demand.
* **Group Vibe Analysis**: Uses Gemma to periodically analyze the group atmosphere (mood, tension, energy) and aligns its response tone accordingly.
* **Frosted Glassmorphic Web Dashboard**: Real-time stats, resource allocation tracker, SQLite user memory card explorer, and OpenRouter model routing manager.
* **Auto-Discovery Worker**: Periodically sweeps OpenRouter for active free models, benchmarks latency and tool-calling capabilities, and ranks them in the registry.
* **Continuous Observation Mode**: Switches to a high-frequency Gemini Live streaming listener if chat volume spikes (>100 messages within 10 minutes) to digest context in the background.
* **Structured Long-Term Memories**: Updates user profiles (interests, skills, projects, facts, relationships) based on conversation statements.

---

## 🛠️ Codebase Structure

The project is structured modularly:
```text
├── data/
│   └── memory.db           # SQLite Database (Autogenerated)
├── public/                 # Web Dashboard Assets
│   ├── index.html          # Dashboard HTML
│   ├── style.css           # Premium Glassmorphic Styling
│   └── app.js              # Dashboard client-side logic
├── src/
│   ├── classifier.js       # Input categorization rules
│   ├── database.js         # SQLite schema, profiles, logs, and registry helpers
│   ├── discovery.js        # OpenRouter free models discovery scheduler
│   ├── planner.js          # Gemma Central Loop & Tool coordination
│   ├── router.js           # Google Gemini / OpenRouter API routing & fallbacks
│   └── tools.js            # Memory, moderation, search, and vision tools
├── .env.example            # Environment variables template
├── .env                    # Active configurations (Gitignored)
├── package.json            # Node.js dependencies
└── server.js               # Main entry point (Express & grammY)
```

---

## ⚙️ Installation & Setup

### 1. Install Dependencies
Ensure you have Node.js 18+ installed, then run:
```bash
npm install
```

### 2. Configure Environment Variables
Copy `.env.example` to `.env`:
```bash
cp .env.example .env
```
Open `.env` and fill in your keys:
* `TELEGRAM_BOT_TOKEN`: Get this from [@BotFather](https://t.me/BotFather).
* `ALLOWED_CHAT_ID`: The ID of your private group. (e.g. `-100123456789`). You can find this by forwarding a group message to a bot like `@ShowJsonBot`.
* `GEMINI_API_KEY_DEFAULT`: Your main Google AI Studio API key.
* `OPENROUTER_API_KEY`: Your OpenRouter API Key (required for Gemma and free model discovery).

### 3. Run Locally
Start the local server and Telegram Bot listener:
```bash
npm start
```
The web dashboard will be available at [http://localhost:8080](http://localhost:8080).

---

## 🌐 Deployment to Render

This application is optimized to run on **Render** (as a Web Service) and be kept active continuously:

1. **Push Code to GitHub**: Put this codebase into your private/public GitHub repository.
2. **Create Render Web Service**:
   * **Environment**: `Node`
   * **Build Command**: `npm install`
   * **Start Command**: `npm start`
   * Add all keys from `.env` to the **Environment Variables** tab in Render.
3. **Uptime Robot Setup**:
   * Create a free account on [Uptime Robot](https://uptimerobot.com/).
   * Create a new **HTTP(s) Monitor**.
   * Point it to your Render web app URL: `https://your-app-name.onrender.com`.
   * Set interval to **5 minutes**. This pings your Express dashboard endpoint regularly, preventing Render's free tier from sleeping.